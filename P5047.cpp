#include <bits/stdc++.h>
using namespace std;

const int MAXN = 1e5 + 10;

vector<tuple<int , int , int> > Vec1[MAXN] , Vec2[MAXN];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    return 0;
}